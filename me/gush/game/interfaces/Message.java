package me.gush.game.interfaces;

public enum Message {
	/*
	 * You can add random messages in here that we will use
	 * For instance we load the message "LobbyCountdown from the config
	 * This message is then stored in messagemanager hashmap with keys Message and values the literal message outputed
	 */
	LobbyCountdown;
}
